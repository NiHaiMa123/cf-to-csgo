# CF 光效移植计划

> 日期：2026-09-15。状态：规划，尚未实施本计划中的新增光效。
> 目标：把 CF 武器光效补入现有 **CS:GO Legacy / Source 1 / MIGI** 移植流程。
> 本文件按用户要求新增，专门记录可选实现路径；已验收武器的活状态仍以 `pipeline.md` 为准。

## 1. 目标与当前基础

光效需要拆成几类处理：

| 类型 | 画面表现 | 优先候选 |
|---|---|---|
| 枪身自发光 | 眼睛、能量槽、纹路在暗处仍可见 | 稀疏遮罩 + 自发光材质 |
| 动态表面光效 | 呼吸、闪烁、流动纹路 | 材质代理、序列帧纹理 |
| 附着光晕 | 枪身外的一小圈光、薄雾、能量片 | 附加网格 + 加法混合材质 |
| 空间特效 | 电弧、火焰、漂浮粒子、拖尾 | Source 1 PCF 粒子系统重建 |
| 动作瞬时特效 | 切枪、开火、换弹、检视时出现 | 挂点 + 动画事件 + 生命周期管理 |
| 照亮环境 | 光投到墙面、地面或其他模型 | 动态灯光，单独评估运行时支持 |

**建议先以天袭为试点，再抽象成其他武器可复用的流程。** 天袭已有明确的 CF 特效入口和验收过的模型、动画、材质，可减少同时排查多个问题的成本；雷神作为第二个验证样本。具体光效外观仍需从 CF 原始资源及参考画面确认。

### 已查证的仓库基础

- `work/galil_ace_tianxi/scan/bute_galilace.json` 已记录 `PVEffectName=pv_galilace_phantombeast_idle`，变换形态另有 `pv_galilace_phantombeast_chg_idle`。这只是特效名称入口，尚不能据此确认其贴图、发射器、挂点或时间曲线。
- `work/galil_ace_tianxi/texture/build_textures_v2.py` 当前为 v7 材质方案，已使用 `$selfillum`、`$selfillummask` 和蓝色 tint；遮罩从 `_M` 的 G/B 通道提取。因此当前缺口不只是“让贴图变亮”。
- `pipeline.md` 记录过 `_M` 没有 alpha、R 全 255，直接作遮罩会让整枪发亮的问题。当前脚本的阈值、增强、模糊是 Source 侧近似参数，不能当作 CF 原公式。
- 已恢复的 `GalilACE_PhantomBeast.CFG` 包含 `OverlayAlphaMapName2`、`OverlayMapName0` 等线索；其 `[Techniques]` 中 `TexturePanningEnabled=0`。不能因为文件存在滚动速度字段，就断言对应效果已启用；还要确认实际使用的 technique 和运行时绑定。
- `work/galil_ace_tianxi/native_vm/build_galilace_vm.py` 已生成 QC 挂点和声音、枪口、抛壳事件，可作为扩展入口；目前没有建立天袭独立光效的完整链路。
- `scripts/csgo_pack/deploy_cf_ak47_beast_migi.py` 有眼部加法材质尝试，只作为实现参考，不代表通用光效管线已经验收。
- `scripts/material_recovery/` 有 CFG、RenderStyle、FXO 的离线研究能力，可复用来分析材质来源；CF shader 字节码不能直接当作 Source 材质使用。

`pipeline.md` 旧条目中的“粒子特效不可移植”在本计划中解释为：**尚无可直接使用的转换链路，需要重建并验证目标引擎能力**。后续有实测证据后再更新该条目，不提前声明还原成功。

## 2. 先查清 CF 光效由什么组成

### 2.1 资源追踪

1. 从 Weapon/Bute 的 `PVEffectName` 出发，搜索完整 REZ 索引和相关配置内容，沿引用找特效定义、子特效、贴图、模型、socket/骨骼及事件名。不能只搜武器同名文件，公共特效可能在共享资源中。
2. 复用 `scripts/cf_extract/extract_all.py` 和现有 `rez_verified_payload.read_verified_payload`，记录包名、内部路径、MD5 校验结果及提取文件 SHA-256。
3. 依据实际文件头和结构判断格式，再决定是否需要特效定义解析器；不预设所有效果都在 `.fx`、`.spr` 或某个固定扩展名中。
4. 把 LTB 内已有的光效 mesh、独立附着模型、材质 overlay 和外部粒子分开记录。若导出时漏掉原有光效 mesh，优先补导出，避免重复造一层。
5. 对所有候选贴图输出 RGB/alpha 分通道预览、尺寸、帧数或图集布局、透明区统计；确认颜色图、遮罩、噪声、序列帧各自用途。

### 2.2 动作与外观取证

- 收集同一武器普通形态的 idle、draw、fire、reload、inspect 参考片段，分别标出常驻效果与动作触发效果。
- 记录颜色、范围、亮度变化、循环周期、出现/消失时间，以及特效跟随枪身还是留在世界空间。
- 对有资源依据的参数标记 `verified`；由画面对齐得到的参数标记 `inferred`；无法读取原定义而手工重建的部分标记 `approximation`。
- 缺少原始录屏或无法解析某字段时，保留缺口；先做可验证的部分，不把凭感觉调出的效果标成原版参数。

建议产物：`work/galil_ace_tianxi/effects/discovery/` 下的 `asset_graph.json`、`texture_channels.json`、`reference_timeline.json`，以及贴图预览和引用链报告。

## 3. 可能的实现路径

以下难度是相对估计；Source 通用文档中的功能仍需在本机 Legacy 构建实测。

### 路径 A：静态自发光材质（低难度，保底）

- 沿用 `VertexLitGeneric` 的稀疏自发光遮罩，只让眼睛、能量纹路等区域发亮。
- 优先使用已验证的原始遮罩；必要时按 UV 区域补人工遮罩，并记录生成方法。
- 独立调节颜色和强度，保持已验收的枪身底色、法线及金属响应。
- 局限：不能单靠这条路径生成枪身外的电弧或真实环境照明；明显的屏幕光晕也需要额外方案或游戏后处理验证。
- 验证重点：暗处可见、亮处不过曝，手臂及普通金属区域没有被一起点亮。

### 路径 B：动态材质（低到中难度）

- 呼吸/闪烁：尝试 `Sine` 等材质代理驱动独立光效材质参数。
- 流动：尝试 `TextureScroll` 驱动光效层的 UV 变换。
- 周期电光：将原始序列帧或离线重建帧转成多帧 VTF，尝试 `AnimatedTexture` 播放。
- **滚动应发生在独立光效层上**，避免移动枪身 diffuse、法线和固定纹路；只有 shader 支持独立目标参数且实测通过时，才复用主材质。
- 局限：时间驱动的循环不等于动作驱动的同步；共享材质的相位、切枪后是否重置、指定参数是否接受代理都要验证，不能假设每把武器有独立时钟。
- 失败回退：保留 A，或转路径 C 的独立网格/材质；不靠持续提高主材质亮度掩盖缺失效果。

Source 材质代理用于动态修改材质参数，具体参数和代理可用性需要按目标分支验证。参考：[Material proxies](https://developer.valvesoftware.com/wiki/Material_proxies?uselang=en)。

### 路径 C：附加光效网格（中难度，优先备选）

- 在现有模型中增加少量光片、窄带、交叉面或薄壳，使用独立材质与透明/加法混合；适合局部光晕、能量片和短距离电光。
- 若 CF 已有该类 mesh，复用其几何、UV 和骨骼权重；否则围绕确认的部位创建简化网格。
- 跟随正确 CF 骨骼；顶点、骨架和挂点统一走现有 H 变换及绕枪身中心镜像流程。按当前导出约定只翻转一次 UV，并处理镜像后的绕序。
- 可与 B 组合：静态绑定网格 + 动态纹理。固定网格不会自动面向相机，需要用多视角检视测试其厚度和方向。
- 对动作可见性单独试验动画驱动方案；不能默认仅写 QC 就能在每个动作精确切换材质或 bodygroup。
- 风险：透明排序、与枪身重叠闪烁、穿过手部、近裁剪、检视角度露出光片，以及多层半透明造成的重复绘制开销。
- 优点：可随模型打包，适合作为粒子加载或触发失败后的近似交付路线。

### 路径 D：Source 1 PCF 粒子重建（高难度，空间特效主路线）

- 根据 CF 特效资源/参考画面，在 Source 粒子工具中重建 sprite、发射频率、生命周期、颜色/尺寸曲线、运动和拖尾；CF 定义与 PCF 不假设存在一对一转换。
- 第一轮只做一个简单粒子，不先还原完整天袭效果。先证明以下链路：
  1. 独立命名的 PCF、VMT、VTF 进入实际加载包；
  2. 当前 Legacy 分支能加载并预缓存该粒子定义；
  3. 一个测试挂点上的粒子可以创建、跟随和停止；
  4. 切枪、动作中断和重新进入 idle 后，没有残留或重复叠加。
- 新增专用挂点，保留现有挂点顺序和索引；当前枪口事件引用挂点 `1`，不能因新增挂点改变原枪口/抛壳行为。
- 可调查 QC 的 `AE_CL_CREATE_PARTICLE_EFFECT` / `AE_CL_STOP_PARTICLE_EFFECT` 路线；文档有基于挂点创建及停止的示例，但当前武器客户端是否处理这些事件、参数格式和停止语义必须实测。参考：[Particles In Animations](https://developer.valvesoftware.com/w/index.php?redirect=no&title=Particles_In_Animations)。
- 从当前游戏资源检查粒子清单和预缓存入口，验证 MIGI 打包规则。若需要清单覆盖，基于 stock 合并并检查其他武器，不能以只含本次效果的清单覆盖全局依赖。
- `migi_tools/migi.exe_extracted/migi-data/addonTemplate/particles/README.TXT` 历史上建议尝试 Alien Swarm Particle Editor；这只是工具候选，需另验编辑器输出与本机 CS:GO 的兼容性。
- 对常驻粒子，重点确认“创建一次、正确恢复、及时停止”；仅在循环 idle 首帧创建，可能每圈叠加，或在动作切换后丢失。
- 前置链路失败时，将失败点记录为加载、预缓存、事件、挂点或生命周期问题；转 C+B 实现近似效果，PCF 路线保留研究状态。

### 路径 E：离线烘焙成序列帧（中难度，复杂效果的折中）

- 将可恢复的 CF 效果或手工重建效果离线渲染为透明序列，再放到 C 的光片或 D 的 sprite 上。
- 适合周期稳定的能量团、短电弧和固定视角局部效果；使用独立小贴图，评估分辨率、帧率、循环接缝、mipmap 及显存占用。
- 局限：只有有限视角，不能完整表现三维绕行、随机发射或世界空间拖尾。普通 CF 游戏录像含背景与遮挡，不默认能够直接提取为干净透明素材。

### 路径 F：运行时控制与动态灯光（高难度，后续扩展）

- 若要求真正照亮环境、读取特殊武器状态，或纯资源包无法处理粒子生命周期，再调查目标分支的实体/脚本/插件能力。
- 地图脚本或服务端方案与 MIGI 资源替换是不同交付条件；先证明能作用到正确的第一人称武器，并确认可用游戏模式。
- 客户端自定义 shader/渲染代码涉及额外运行时支持，不能认为放一个 DLL、FXO 或 VMT 到 addon 就能生效。
- 本轮先不选这条路线；若证明确有必要，再单列实现范围、部署依赖和验证方式。

## 4. 推荐组合与选择规则

| 要恢复的效果 | 首选 | 回退 | 升级条件 |
|---|---|---|---|
| 固定能量纹路/眼睛 | A | 人工校准的稀疏遮罩 | 原版确有呼吸变化时加入 B |
| 枪身表面流光 | B+C 的独立光效层 | 静态光片 | UV 运动与原版不符时尝试 E |
| 贴近枪身的光晕 | C+B | C 的静态透明层 | 需要视角朝向或空间运动时尝试 D |
| 漂浮粒子、电弧、拖尾 | D | C+E 的近似表现 | 资源包无法管理状态时评估 F |
| 动作瞬时闪光 | D + 动画事件 | 经验证的 C 动画方案 | 必须读取特殊运行时状态时评估 F |
| 环境照明 | F 的独立能力试验 | 只保留武器可见光效，明确差异 | 运行时入口通过后再实施 |

**推荐顺序：资源取证 → 保留 A 基线 → 并列评估 B/C 与 D 的最小样例 → 选择组合 → 动作集成 → 打包验收。** 不把“整枪变亮”当成空间光效还原，也不要求先解析完所有 CF 特效格式才能推进一个试点。

## 5. 实施阶段与产物

| 阶段 | 工作 | 产物与通过条件 |
|---|---|---|
| E0 基线 | 记录当前天袭 addon、模型、材质、声音及实际游戏版本 | `baseline/manifest.json`、关键 hash、对比画面；可恢复现有验收版本 |
| E1 取证 | 完成资源引用链、通道检查、外观与时序分类 | `discovery/`；明确已知参数、未知字段和近似部分 |
| E2 能力试验 | 分别做呼吸材质、单光片、单粒子创建/跟随/停止 | `probes/`；每条路线有独立结果和失败原因，优先判断 D 是否可用 |
| E3 外观重建 | 先恢复普通形态 idle 的一个主要效果，再补其他层 | `build/`、配置及同条件对比；效果位置/范围/节奏可调且可复现 |
| E4 动作集成 | 加入必要的 draw/fire/reload/inspect 特效，处理动作中断 | `timeline.json`、事件表、实测录像；无常驻粒子堆积或切枪残留 |
| E5 打包 | staging → 实际 MIGI addon → 用户 REBUILD → pak 检验 | `deploy/` 三层 hash；确认新增文件实际被游戏读取 |
| E6 验收与推广 | 视觉、动作、性能回归，用户确认；再推广雷神 | `acceptance.json`；更新 `pipeline.md` 的光效状态与实际回退 |

上述目录均位于 `work/galil_ace_tianxi/effects/`，目前只规划，不创建伪造的通过报告。

### 建议的后续代码组织

- 武器配置：`assets/weapons/galil_ace_tianxi/effects.json`，记录原始引用、层类型、Source 实现、贴图、骨骼/挂点、局部变换、曲线、触发/停止规则及证据等级。
- 资源调查与生成脚本：后续按已确认的格式放入 `scripts/weapon_port/`，避免把天袭路径写死进通用转换器。
- 模型集成：扩展现有 `build_galilace_vm.py` 的独立光效 mesh、挂点和事件生成；时序沿用现有 `times_ms` 到输出序列帧的映射，若序列重定时则同步调整事件。
- 校验：复用 `scripts/weapon_port/validate_materials.py` 检查 SMD → VMT → VTF；另补 PCF → 子效果/材质、清单加载、挂点存在与触发/停止配对检查。现有材质校验器不覆盖 PCF 或运行时行为。
- 输出开关：至少支持关闭全部新增光效、仅材质、材质+网格、包含粒子四种构建模式，便于定位问题和回退。

## 6. 验收标准与回退

- **外观**：在明亮/阴影背景及正常持枪/检视角度对比；发光区域正确，无整枪泛白、错误染色、黑边、穿模或明显光片边缘。
- **跟随**：idle、跑动、开火、换弹、切枪、检视时光效随正确部位运动；确认局部粒子与世界空间拖尾各自符合参考。
- **生命周期**：连续触发至少 20 次开火/检视/切枪，并测试换弹中断、死亡重生；没有残留、重复生成或越叠越亮。20 次是本计划的回归样本量，不是引擎限制。
- **基础回归**：原声音、动画、枪口闪光、抛壳及其他武器不受影响；本轮沿用第一人称范围，第三人称文件 hash 保持基线。
- **性能**：相同地图、视角和设置下比较光效开/关的帧时间；记录峰值粒子数量、透明层数和纹理体积。先量基线再定预算，不以单次平均 FPS 宣称无开销。
- **部署**：按 `pipeline.md` 的 A/B/C 三层链路核对新增和修改文件。MIGI REBUILD 由用户手动执行；pak 未核验时保持 `PACK_VERIFY_PENDING`。
- **回退**：保留基线 manifest 和本轮增量文件清单；只恢复本轮改动并撤回明确新增的资源，不清理或重建 `data/`。CF 原始资源保持 local-only。
- **完成定义**：用户在实际游戏中确认选定效果；任何尚未恢复的空间层、变换形态或动态照明都明确列为回退，不能仅凭材质编译成功标记全部完成。

## 7. 下一步最小任务

1. 以 `pv_galilace_phantombeast_idle` 为入口，定位真实特效定义和被引用资源，形成第一版资产图。
2. 确认哪些光效已经包含在 LTB mesh / CFG overlay 中，哪些必须独立生成。
3. 在天袭试验构建中验证“一个粒子能加载、跟随一个挂点并可靠停止”。
4. 根据试验结果，选 **A+B+C+D** 的完整组合，或先交付 **A+B+C/E** 的近似组合；记录差异再进入正式实现。
