# Authorized implementation task: first material-only experimental variants

You are the requested Grok Build grok-4.6 high specialist. Coordinator makes scope and design decisions. User now explicitly said START executing plan.material-rendering.md, superseding its former plan-only status. They confirmed position/FOV are fine: ONLY weapon materials/rendering.

Read plan.material-rendering.md and relevant existing material translator and generated VMTs. Implement a SMALL Python standard-library experiment generator at work/mauser_libra/material_rendering_fix/build_experiments.py. Your ONLY write scope is that file. Use your native patch/edit tool, not shell write tricks. No changes to generic translator, baseline, source_v2, addon_v2, models, UV, animations, hands, game installation, Git/config, or plan. No deployments or GUI. No further subagents. Do not read credentials. Do not perform broad investigations; finish the bounded code task within 15 tool calls and return summary. If unable to edit, return full code as final.

The coordinator has another worker creating baseline/snapshot files. You must not touch those. Design generator CLI --baseline <snapshot addon dir> --out <new output dir> (never invent current snapshot name). Baseline is a frozen full addon copy. Require --out absent or empty; never overwrite populated evidence. Copy all baseline files to each experiment addon so full file inventory remains compatible. Validate relative paths/no symlink escapes; no destructive filesystem behavior. Every output is under task experiment root. Record SHA256 baseline and candidate files and exact diffs in machine-readable manifest. Zero changes allowed outside exact VMT allowlist, assert all other assets byte-identical. Output immutable per-experiment directories.

Only two independent variants, both derived directly from baseline:
M1_r5_phong: ONLY cf_mauser_libra_r5.vmt changes. Existing r5 has $phong 0. Restore controlled Phong using the exact current r0 high-level Phong keys (phong, basemapalphaphongmask, phongexponent, phongboost, phongfresnelranges, phongtint) while preserving r5 base/bump/other original keys. No envmap, no new VTF, no global tint edits. Treat switch from no spec to existing controlled spec preset as one bounded factor. Assert r0 exists and uses expected VertexLitGeneric keys; don't hard-code undocumented settings.
M2_env_off: ONLY r1/r4 VMTs change; remove $envmap, $normalmapalphaenvmapmask, $envmaptint while preserving all other fields. This is one environment-feature disable ablation. No other variants yet.

For each manifest state generation_validation only, runtime_status=PENDING; explicitly no visual PASS. Include hypothesis, changed factor, source/candidate hashes, changed file list, unchanged count, parameter diff and expected observations. Add short README strings emitted by the script explaining these are ablations, not final fix, and use unchanged FOV, position, animation, hands. Do not generate screenshot/control commands or manipulate game settings.

Robustly parse simple shipped VMTs; fail closed on duplicate keys, unexpected structures, absent required keys, or malformed paths. Prefer a short auditable script. No external dependencies. Tests should be meaningful: script's byte-level assertions and CLI dry-run/help are sufficient now. Coordinator will execute generator and independently review resulting diffs. Finish with the exact edited file path and invocation.
